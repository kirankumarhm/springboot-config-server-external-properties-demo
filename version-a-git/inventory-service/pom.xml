<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example.config</groupId>
    <artifactId>version-a-git</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>inventory-service</artifactId>
  <name>inventory-service</name>
  <description>Config client 1 - proves scoped refresh and feature-flag driven behaviour</description>

  <dependencies>
    <dependency>
      <groupId>com.example.config</groupId>
      <artifactId>config-client-commons</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-config</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-bus-amqp</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!--
      Startup retry against the Config Server (NFR-14) needs NO extra dependency: the retry
      parameters ride on the spring.config.import URI and are handled by the config-data
      implementation. spring-retry + spring-boot-starter-aop are required only by the LEGACY
      bootstrap path, which this project does not use (CON-06) - and spring-boot-starter-aop
      was removed in Spring Boot 4 anyway (last GA release is 3.5.16).
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      Spring Boot 4 split the test slices into per-technology modules, so @WebMvcTest no longer
      arrives with spring-boot-starter-test as it did in Boot 3.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
